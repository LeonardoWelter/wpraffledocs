[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "WP Raffle"
authors = [{name = "Leonardo Welter", email = "leonardowelter@email.com"}]
dynamic = ["version", "description"]
